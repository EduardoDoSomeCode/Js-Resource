const invertNumber = (number) => {
    let stringNumber = parseInt(number.toString().split("").reverse().join(""))  * Math.sign(number)
    return stringNumber
}

//Edge caise los numeros que tiene al final 0 no registra el cero como tal 

const invertNumberTwo =(number)=>{


    for (let i = 0; i < array.length; i++) {
        const element = array[i];

    }
}
console.log(invertNumber(-68)) // 06